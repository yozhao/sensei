<?xml version="1.0" encoding="UTF-8"?>
<schema xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://javasoze.github.com/sensei/schema/sensei-schema.xsd">

	<!-- valid types: uid - only 1 column can be of type uid int - integer short 
		- short char - character long - long float - float double - double string 
		- string (default) date - date, format string must be provided text - searchable -->

	<table uid="id">
		<column name="userId" type="int" />
		<column name="msgTime" type="long" />
		<column name="msgAdd" type="string" />
		<column name="msgType" type="short" />
		<column name="msgId" type="string" store="YES" />
		<!-- attributes: indexed, termvector are only used when type is text -->
		<column name="contents" type="text" index="ANALYZED" store="NO" termvector="NO" />
	</table>

    <facets>
		<facet name="msgTime" type="simple" />
	</facets>
</schema>
