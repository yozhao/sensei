Tools:

IndexSplitter:

this tool repartitions an existing index

Usage:

./bin/split.sh $1 $2 $3 $4

where

$1 - src index directory
$2 - max partition id
$3 - target directory that should contain the indexes
$4 - comma delimited partitions, e.g. 0,1,3

